# the `[release] vX.Y.Z` is merged to `main` now

# publish to crates.io, pypi.org and go registry

# create a tag `vX.Y.Z`

# create a github release with change log

# for now, there is not asset provided on github releases
